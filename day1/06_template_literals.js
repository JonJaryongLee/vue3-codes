const myName = "david";
const myAge = 30;

// + 연산자 사용
console.log("이름은 " + myName + "입니다.");
console.log("나이는 " + myAge + "입니다.");

// template literals
console.log(`
  이름은 ${myName}입니다.
  나이는 ${myAge}입니다.
`);
