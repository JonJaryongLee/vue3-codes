function sayHello(name, age) {
  return `안녕, ${name}. 나이는 ${age}세`;
}
const helloText = sayHello("david", 30);
console.log(helloText); // 안녕, david. 나이는 30세

// const sayHello = (name, age) => {
//   return `안녕, ${name}. 나이는 ${age}세`;
// };
// const helloText = sayHello("david", 30);
// console.log(helloText); // 안녕, david. 나이는 30세
